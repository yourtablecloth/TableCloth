<Window x:Class="Host.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Host"
        mc:Ignorable="d"
        Title="식탁보 자동 설치 프로그램" Width="640" Height="480">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="300*"/>
            <RowDefinition Height="40*"/>
        </Grid.RowDefinitions>
        <TextBlock Padding="10" TextWrapping="WrapWithOverflow" FontWeight="UltraBold" FontSize="20" Background="BlueViolet" Foreground="White" Grid.ColumnSpan="2" Grid.Row="0">
            식탁보 자동 설치 프로그램
        </TextBlock>
        <WrapPanel Orientation="Vertical" Grid.ColumnSpan="2" Grid.Row="1">
            <TextBlock HorizontalAlignment="Stretch" Padding="10" TextWrapping="WrapWithOverflow">
                이 프로그램은 식탁보 프로그램에서 전달받은 설치 프로그램 내역을 일괄적으로 설치해주는 도구입니다. 다음의 프로그램을 자동으로 설치할 것입니다.
            </TextBlock>
            <ListBox HorizontalAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Visible">
                <ListBox.Items>
                    <Label Foreground="Green">✔️ Succeed</Label>
                    <Label Foreground="DarkOrange">⚠️ Warning</Label>
                    <Label Foreground="Red">❌ Failed</Label>
                </ListBox.Items>
            </ListBox>
        </WrapPanel>
        <WrapPanel HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0">
            <Button Margin="10 0 0 0">프로그램 정보</Button>
        </WrapPanel>
        <WrapPanel HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="2" Grid.Column="1">
            <Button Margin="0 0 10 0">설치하기</Button>
            <Button Margin="0 0 10 0">프로그램 닫기</Button>
        </WrapPanel>
    </Grid>
</Window>
