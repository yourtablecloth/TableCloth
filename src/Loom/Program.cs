/*
Synopsis

wsbx.exe
  build
    --virtual-gpu [yes | no]
    --networking [yes | no]
    --map [host] [sandbox] [readonly]
    --startup-script [file path]
    --audio-input [yes | no]
    --video-input [yes | no]
    --protected-client [yes | no]
    --printer-redirect [yes | no]
    --clipboard-redirect [yes | no]
    --memory [size in mb]
    --add [host]
    [--force]
    output [zip file path]

  run
    --working-directory [directory path]
    --no-wait
    zip file path

  help
  version
 */

public static class Program
{
    public static void Main(string[] args)
    {
    }
}